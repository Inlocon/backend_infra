# NOT NECESSARY
# (just saving the same images twice in 2 different repos doesn't make sense - only costs money)

# module "webservice_ecr_repo" {
#   source = "../../modules/5_ecr"
#   name   = "webservice"
#   env    = var.env
# }
#
# module "dbsync_ecr_repo" {
#   source = "../../modules/5_ecr"
#   name   = "dbsync"
#   env    = var.env
# }
